/**
 * Tailwind v4 uses the @tailwindcss/postcss plugin and @theme tokens.
 * Content paths are not required anymore. Keep darkMode for class-based theming.
 */
module.exports = {
  darkMode: 'class',
};
